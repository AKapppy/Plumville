Read AGENTS.md, notes.txt, file_priority.txt, CODEX_TASK.md, and
QA_REPORT_TEMPLATE.txt before changing anything.

Work on Desktop Stabilization Run D01-A only.

Important: the latest checkout is the source of truth. It already contains
desktop_improvements.py and ui_extensions.py applies it, even though an earlier
generated updater failed. Do not run or reapply old updater scripts. Audit the
current code in place.

First:
1. show the current branch and `git status --short`;
2. run baseline Python, npm, path, and Tk smoke checks;
3. launch `python3 plumville_app.py` and capture the complete error or broken
   behavior;
4. inspect extension application order and method wrappers;
5. identify and fix the smallest root cause;
6. add regression coverage;
7. rerun the relevant checks.

Follow all repository safety rules:
- do not commit;
- do not push;
- do not reset or clean the worktree;
- preserve ignored/private/runtime data;
- do not modify phone/mobile-specific public behavior;
- avoid blind whole-file replacements;
- keep lines reasonably short.

As you work:
- mark the active checklist item `[~]`;
- mark automated implementation items `[x]` only after their tests pass;
- leave all user verification boxes unchecked;
- update notes.txt and file_priority.txt at the end of the run.

Stop after D01-A is coherent. Do not begin the next major desktop phase.

End with the exact structure in QA_REPORT_TEMPLATE.txt, including a numbered
list of things I should manually check.
