Minecraft Metro Map Notes
=========================

#Web Version
   [x] clicking outside of the station popup box should close it
   [x] get rid of world map rectangle

#World Map
   [x] there's a lot of visual noise going on. we should be doing a block-by-block read of the terrain, not stopping at chunk-by-chunk
   [] this should be a completely virtual experience. no minecraft world will be opened at anytime to generate the terrain. it should just be based off the seed and the seed alone. if docker desktop is looking for an open or saved minecraft file, it will never find one. we should reroute it if this is the case
   [] keep filling the rectangle with repeated Auto Fill Step clicks.
      - active generated world folder: `metro_map_world_boxfill`
      - render area is centered on Blackport at x=294, z=390, radius=2000
      - Auto Fill now scans saved chunk columns and targets the smallest under-filled Blackport box
      - coverage now includes cached headless packet columns as well as LevelDB columns
      - latest Auto Fill backend pass targeted 12 under-filled points: (-474, -378), (678, -378), (-474, 6), (1062, 774), (-90, 1158), (294, -378), (1062, 1158), (-474, 774), (1062, -378), (678, 1158), (-474, 390), and (-474, 1158)
      - next target scan now sees 14 under-filled targets around the first Blackport box
      - Auto Fill Step is the only world-map button; it runs one longer Bedrock load session, then renders
      - current load session settings: wait 75 seconds, up to 12 teleport targets
      - latest focused backend load cached 1083 level-chunk packets, 1072 unique chunk columns, and 8043 subchunk payloads
      - latest render reads 12260 chunk columns and draws 7984 known-color pixels
      - latest rendered terrain bounds: x -926..1518, z -830..1614
      - default gray / unknown blocks are now transparent instead of rendered, so the map is intentionally sparse until more network palette IDs are mapped back to block names
      - unknown/uncolored block details are written to `worldgen_output/uncolored_blocks_report.txt` after each render
      - latest report found 560 uncolored block entries and 63799308 sampled uncolored occurrences
      - latest render has 15 subchunk decode errors
      - headless loader now saves `headless_start_game.json` so the next renderer pass can inspect whether Bedrock is using hashed block network IDs and which block names were advertised
      - do not repair or edit the live Bedrock DB before Auto Fill; the old recovered world crashed after repeated restarts plus live DB mutation


Future Items
------------
[] World map: expand chunk loading across the full requested render area, not just the first patch around Blackport.
[] World map: if server-side teleports do not persist new chunk areas, make the headless client move itself or reconnect at the next target.
[] World map: add a focused preview or "zoom to rendered area" helper if the full-radius render stays hard to inspect.
[] World map: improve the terrain palette after coverage is reliable.
[] World map: consider moving generated world data out of iCloud Drive if performance or sync weirdness becomes a problem.
[] World map: remember that changing the seed later will not magically regenerate an existing saved world.
[] World map: remember that Bedrock only saves chunks that have actually been generated/visited.
