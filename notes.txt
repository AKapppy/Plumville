PLUMVILLE MASTER CHECKLIST AND DEVELOPMENT NOTES

Last audited against GitHub main: 2026-07-29
Audited commit: 03f67070beb8224e426a575fd905b791d496810a

======================================================================
ACTIVE CHECKLIST — THIS SECTION IS AUTHORITATIVE
======================================================================

STATUS LEGEND

[ ] Not started
[~] In progress in the current Codex run
[x] Implemented and supported by automated checks
[verify] Implemented but still needs Andrew's live desktop verification
[blocked] Blocked by a concrete dependency
[hold] Intentionally paused
[deferred] Deliberately postponed
[info] Reference information

CHECKLIST RULES

- Codex may mark implementation and automated-test items [x].
- Codex must not mark Andrew's manual checks [x].
- Manual checks remain [ ] until Andrew reports the result.
- A visual feature is not complete because a helper or color constant exists.
- A visual feature is complete only after the actual Tk interface is inspected.
- Update this file and file_priority.txt after every implementation run.
- Do not add unrelated features while a visual slice is in progress.
- Do not commit or push unless Andrew explicitly requests it.

----------------------------------------------------------------------
A. CURRENT PHASE — DESKTOP VISUAL REBUILD
----------------------------------------------------------------------

GOAL

Make the private desktop application structurally and visually match the
Modern Minecraft Control Panel mockups in:

- data/mockups/ChatGPT Image Jul 25, 2026 at 02_28_32 PM (1).png
- data/mockups/ChatGPT Image Jul 25, 2026 at 02_28_32 PM (2).png
- data/mockups/ChatGPT Image Jul 25, 2026 at 02_28_32 PM (3).png

The mockups are visual references, not permission to remove functionality.

FREEZE DURING THIS PHASE

[hold] New public/mobile features
[hold] New construction calculations
[hold] New alignment or path-editing features
[hold] New metro-turn or endpoint-editing features
[hold] Canonical/public data migration
[hold] Broad legacy_core.py refactoring
[hold] Additional popup micro-adjustments before the docked inspector exists

----------------------------------------------------------------------
B. VISUAL RUN V01 — WORKSPACE SHELL
----------------------------------------------------------------------

IMPLEMENTATION

[x] Read all three mockup images before editing.
[blocked] Capture a current desktop screenshot for before/after comparison.
[verify] Add a real desktop workspace shell rather than another sidebar filter.
[verify] Add a fixed top application bar.
[verify] Add a compact left mode rail.
[verify] Add a mode-specific secondary control panel.
[verify] Keep the map as the flexible center workspace.
[verify] Add an empty but functional right docked inspector region.
[verify] Add a compact bottom/status strip only when it carries useful status.
[verify] Preserve existing window resizing.
[verify] Preserve map zooming, panning, selection, route display, and editing.
[verify] Preserve all existing sidebar controls by moving or mounting them.
[verify] Do not duplicate controls in old and new containers.
[x] Do not delete old builders until the new shell is proven stable.
[verify] Keep the workspace usable at smaller desktop window sizes.
[x] Add focused tests for region creation and mode switching.
[x] Update this checklist after the run.
[x] Update file_priority.txt after the run.

USER VERIFICATION — LEAVE UNCHECKED

[ ] The application opens without a traceback.
[ ] The interface has a clear top bar, left mode rail, center map, and right inspector.
[ ] The interface no longer feels like one endless sidebar.
[ ] All mode buttons are visible without scrolling.
[ ] Selecting a mode clearly changes the secondary panel.
[ ] The map remains the dominant visual area.
[ ] The right inspector can collapse or hide when more map space is needed.
[ ] Existing search, directions, editing, world, and advanced tools remain reachable.
[ ] Resizing the desktop window does not overlap or lose regions.
[ ] The result is visibly closer to the supplied mockups.

----------------------------------------------------------------------
C. VISUAL RUN V02 — DOCKED STATION INSPECTOR
----------------------------------------------------------------------

IMPLEMENTATION

[verify] Replace persistent selected-station popup content with a docked inspector.
[ ] Keep hover tooltips lightweight and temporary.
[ ] Add inspector tabs:
    - Overview
    - Construction
    - Lines
    - Signs
    - Actions
[verify] Add station name, abbreviation, coordinates, and status header.
[verify] Show line-colored diamond badges with line letters in the station
    inspector header.
[ ] Keep edit and destructive actions separated.
[ ] Put destructive actions in a redstone-styled danger region.
[verify] Keep line management behind one coherent Lines or Actions workflow.
[verify] Preserve all existing station actions.
[verify] Preserve selected-station focus on the map.
[x] Keep the inspector synchronized when selection changes.
[verify] Define an empty-selection state that explains how to select a station.
[x] Add focused state and rendering tests.
[x] Update this checklist and file_priority.txt.

USER VERIFICATION — LEAVE UNCHECKED

[ ] Selecting a station fills the right inspector.
[ ] Station name and abbreviation are easy to read.
[ ] Line circles are compact and correctly colored.
[ ] Overview presents the most useful information first.
[ ] Construction status is understandable without opening another dialog.
[ ] Editing actions remain reachable.
[ ] Destructive actions are clearly separated.
[ ] The old floating popup no longer blocks the map.
[ ] Inspector content does not become excessively tall or cramped.
[ ] The inspector feels like the mockup rather than a restyled Tk popup.

----------------------------------------------------------------------
D. VISUAL RUN V03 — COMPONENT SYSTEM AND SIDEBAR CONTENT
----------------------------------------------------------------------

IMPLEMENTATION

[ ] Move MMCP tokens into a dedicated desktop theme/component module.
[ ] Stop scattering web-derived constants across feature logic.
[ ] Create reusable components:
    - app bar
    - mode button
    - panel
    - section header
    - field
    - primary button
    - secondary button
    - warning button
    - danger button
    - status chip
    - line badge
    - station diamond
    - empty state
[ ] Use consistent 4 px / 8 px spacing increments.
[ ] Use crisp rectangles and tiny corner radii.
[ ] Use raised/inset borders sparingly.
[ ] Use modern readable body typography.
[ ] Reserve mono/pixel styling for short labels, coordinates, and headings.
[ ] Add visible hover, pressed, focus, selected, disabled, success, warning,
    and danger states.
[ ] Preserve keyboard focus and tab order.
[ ] Replace generic auto-styling heuristics with explicit component usage.
[ ] Update tests for component configuration.
[ ] Update this checklist and file_priority.txt.

USER VERIFICATION — LEAVE UNCHECKED

[ ] Controls look consistent across every mode.
[ ] Primary actions are visually obvious.
[ ] Danger actions are unmistakable but not visually overwhelming.
[ ] Text is readable at normal macOS display scaling.
[ ] Focus states are visible.
[ ] Buttons do not look like generic stock Tk widgets.
[ ] The interface uses Minecraft influence without looking like a novelty skin.

----------------------------------------------------------------------
E. VISUAL RUN V04 — DIALOGS AND EDITING FLOWS
----------------------------------------------------------------------

IMPLEMENTATION

[ ] Inventory all active desktop dialogs.
[ ] Group dialogs into:
    - small confirmation
    - simple form
    - chooser
    - multi-step editor
    - preview editor
[ ] Create explicit templates instead of recursively styling arbitrary widget trees.
[ ] Restyle Add Station.
[verify] Restyle station rename/edit.
[ ] Restyle line add/switch/remove.
[ ] Restyle alignment editor.
[verify] Restyle metro-turn editor.
[verify] Restyle endpoint editor.
[ ] Keep preview areas visually separate from forms.
[ ] Put Save/Apply actions consistently at bottom right.
[ ] Put Cancel consistently beside Save/Apply.
[ ] Put destructive actions on the opposite side or in a danger area.
[ ] Preserve keyboard submission and Escape/cancel behavior.
[ ] Ensure dialogs size to content without clipping titles.
[ ] Update focused tests and manual-check lists.
[ ] Update this checklist and file_priority.txt.

USER VERIFICATION — LEAVE UNCHECKED

[ ] Dialogs have consistent headers, fields, spacing, and action rows.
[ ] Dialog titles are never clipped.
[ ] Forms are not excessively wide or tall.
[ ] Preview editors are understandable.
[ ] Save and Cancel are consistently located.
[ ] Keyboard navigation remains usable.
[ ] No existing editor loses functionality.

----------------------------------------------------------------------
F. VISUAL RUN V05 — MAP PARITY
----------------------------------------------------------------------

IMPLEMENTATION

[ ] Move desktop station diamonds into canonical map rendering.
[ ] Stop relying on a decorative redraw overlay when practical.
[ ] Keep ordinary stations as diamonds.
[ ] Keep junctions as larger or double diamonds.
[ ] Keep terminals diamond-first.
[ ] Give Mt. Phosphagos a permanent gold/purple landmark treatment.
[ ] Use selected diamond-blue focus rings.
[verify] Use circular line badges only for line identity.
[ ] Preserve dotted/planned versus solid/open meaning.
[verify] Add desktop semantic label priority.
[verify] Prefer abbreviations when full names do not fit.
[verify] Keep route endpoints and selected stations high priority.
[verify] Keep labels collision-aware.
[ ] Measure redraw performance before adding caching.
[x] Update tests and manual checks.
[x] Update this checklist and file_priority.txt.

USER VERIFICATION — LEAVE UNCHECKED

[ ] Station and line symbols cannot be confused.
[ ] Junctions are obvious.
[ ] Mt. Phosphagos is distinctive.
[ ] Selected stations are easy to find.
[ ] Labels remain readable at world, regional, and close zoom.
[ ] Planned and open track remain distinguishable.
[ ] Map interaction remains smooth.

----------------------------------------------------------------------
G. CODEX EFFICIENCY RULES FOR THE VISUAL PHASE
----------------------------------------------------------------------

[ ] One visual run per Codex session.
[ ] Maximum normal production scope: 2–4 production files.
[ ] Tests and tracker files do not count against that limit.
[ ] Do not combine shell, inspector, dialogs, and map rendering in one run.
[ ] Do not add a new feature merely because related code was encountered.
[ ] Do not rewrite legacy_core.py wholesale.
[ ] Prefer a small stable hook over another broad monkey patch.
[ ] Show a diff-stat before and after the run.
[ ] Stop when acceptance criteria for the current slice are met.
[ ] Provide Andrew a short numbered manual-check list.
[ ] Do not mark those manual checks complete.
[ ] Do not commit or push.

======================================================================
VISUAL RUN HISTORY
======================================================================

2026-08-05 — Repository Cleanup Session

- Scope: remove obsolete root-level visual refresh, audit, and AI prompt files;
  clear generated macOS/Python/test cache files outside protected runtime areas;
  preserve active task docs, runtime/private data, exports, history, and all
  `data/mockups/` images.
- Initial branch/status: `main`; `git status --short` already showed modified
  desktop/map/export/test files plus existing `docs/metro_network.json` and
  `priority_list.csv` changes, which were preserved.
- Implementation: deleted old July visual/audit package files after confirming
  they only referenced each other, and removed `.DS_Store`, `__pycache__`, and
  `.pytest_cache` artifacts outside protected runtime folders.
- User verification still required: confirm the remaining root documentation set
  feels sufficient and no old planning file Andrew still wanted was removed.

2026-08-05 — Desktop Identity Edit, Coordinate Search, And Mode Dropdown

- Scope: add abbreviation editing to the station Name action, keep the map
  viewport center stable when the inspector is shown or hidden, remove the
  search instruction text, let coordinate searches place a cursor crosshair,
  and move mode selection from the left rail into the top-bar mode dropdown.
- Initial branch/status: `main`; `git status --short` already showed modified
  desktop/map/export/test files plus existing `docs/metro_network.json` and
  `priority_list.csv` changes, which were preserved.
- Versions: Python 3.13.7; Node v24.11.1; npm 11.6.2.
- Baseline checks before editing: targeted Python compile passed; full
  `python3 -m unittest discover -s tests` failed in the existing
  `tests/test_station_signage.py` Line C/signage expectations; `npm run
  test:paths` passed; `npm test` passed; `python3 plumville_app.py` and Tk
  smoke both aborted with exit code 134 before app-level output.
- Implementation: station Name now opens a two-field dialog for Name and
  Abbreviation, and blank abbreviation clears the saved `abbr` field. Empty
  search no longer shows the long instruction sentence. Searching for a
  coordinate with no station match now centers that coordinate and shows the
  cursor crosshair/readout. Inspector show/hide now refreshes canvas dimensions
  after layout and recenters the same world point that was at the viewport
  center, without changing zoom. The workspace mode control is now a top-bar
  dropdown; the old left mode rail is hidden while the existing bridge API
  remains idempotent.
- Regression coverage: `tests/test_core_network.py` covers abbreviation save
  and clear plus identity-field normalization. `tests/test_desktop_improvements.py`
  covers blank search text and coordinate-search crosshair placement.
  `tests/test_desktop_workspace.py` covers hidden mode rail, dropdown menu
  labels, active dropdown text, and inspector center restore.
- Checks after editing: targeted `python3 -m unittest tests.test_desktop_workspace
  tests.test_desktop_improvements tests.test_core_network
  tests.test_desktop_inspector` passed; targeted Python compile passed; full
  `python3 -m unittest discover -s tests` still fails only in the pre-existing
  `tests/test_station_signage.py` cases; `npm run test:paths` passed; `npm
  test` passed; `git diff --check` passed.
- Live-Tk limitation: `PLUMVILLE_RUN_TK_SMOKE=1 npm run smoke:tk` and
  `python3 plumville_app.py` still abort with exit code 134 before app-level
  output in this execution environment.
- User verification still required: confirm Name shows Name and Abbreviation
  fields, blank abbreviation clears the abbreviation, inspector show/hide keeps
  the same center without zooming, coordinate search shows the crosshair, and
  modes are chosen from the top-bar dropdown instead of left-side tabs.

2026-08-05 — Desktop Map Labels, Line Markers, And PNG Export

- Scope: convert the desktop map export affordance from SVG to PNG, add
  desktop label abbreviation/collision logic based on the web viewer, draw
  circular line-letter markers on metro lines, make inspector line diamonds use
  white borders and letters, and align unnamed-station detection with the web
  placeholder rule for examples such as `Z_2` and `UW`.
- Initial branch/status: `main`; `git status --short` already showed modified
  `docs/metro_network.json`, `file_priority.txt`, `notes.txt`,
  `priority_list.csv`, which were preserved.
- Versions: Python 3.13.7; Node v24.11.1; npm 11.6.2.
- Baseline checks before editing: targeted Python compile passed; full
  `python3 -m unittest discover -s tests` failed in existing
  `tests/test_station_signage.py` Line C/signage expectations; `npm run
  test:paths` passed; `npm test` passed; `python3 plumville_app.py` and Tk
  smoke both aborted with exit code 134 before app-level output in this
  execution environment.
- Implementation: the desktop Export button/dialog now says Export PNG and
  writes `metro-map-*.png` by rasterizing the existing validated map SVG bytes
  through CairoSVG. The main map redraw now stages station label placement,
  tries abbreviations before full names away from close zoom, keeps selected
  stations/route endpoints/junctions/terminals ahead of ordinary labels, skips
  crowded low-priority labels, and draws circular line markers for terminal
  lines and zoomed-in junction line identity. Inspector line diamonds now use
  white outlines and white letter text.
- Regression coverage: `tests/test_public_export.py` covers PNG rasterization
  from validated SVG bytes. `tests/test_path_rendering.py` covers abbreviation
  preference, route-endpoint label priority, and circular line-marker drawing.
  `tests/test_desktop_inspector.py` covers white inspector diamond outline/text.
  `tests/test_core_text.py` covers `Z_2` and `UW` placeholder/unnamed handling.
- Checks after editing: targeted `python3 -m unittest tests.test_path_rendering
  tests.test_public_export tests.test_desktop_inspector tests.test_core_text
  tests.test_desktop_improvements` passed; targeted Python compile passed; full
  `python3 -m unittest discover -s tests` still fails only in the pre-existing
  `tests/test_station_signage.py` cases; `npm run test:paths` passed; `npm
  test` passed; `git diff --check` passed.
- Live-Tk limitation: `PLUMVILLE_RUN_TK_SMOKE=1 npm run smoke:tk` still aborts
  with exit code 134 before app-level output in this execution environment.
- User verification still required: confirm Export PNG writes an openable PNG,
  crowded station labels prefer abbreviations or drop low-priority overlaps,
  line-letter circles appear on metro lines without clutter, and inspector
  line diamonds have white borders and white letters.

2026-08-04 — Unassociated Station Data Cleanup

- Scope: remove the screenshot-confirmed unassociated station records at
  `(-5080, 3432)`, `(5144, 2328)`, and `(5232, -2456)` from exported metro
  data and the priority list.
- Initial branch/status: `main`; `git status --short` already showed modified
  `docs/metro_network.json` and `priority_list.csv`, which were preserved and
  edited in place.
- Versions: Python 3.13.7; Node v24.11.1; npm 11.6.2.
- Baseline checks before editing: targeted Python compile passed; full
  `python3 -m unittest discover -s tests` failed in existing
  `tests/test_station_signage.py` expectations around Line C signage data;
  `npm run test:paths` passed; `npm test` passed.
- Implementation: removed station vars `P_0`, `P_6`, and `P_2` from
  `docs/metro_network.json`, removed three alignment reminders pointing at
  deleted station vars, removed their three `priority_list.csv` rows, and
  renumbered the remaining priority ranks.
- Checks after editing: JSON parsing passed; a focused data check found no
  remaining `P_0`/`P_6`/`P_2` station records, removed coordinates, alignment
  endpoints, or priority rows, and priority ranks are continuous through 78.
  The full Python suite still fails in existing `tests/test_station_signage.py`
  expectations unrelated to this cleanup.
- User verification still required: confirm the public/desktop priority view no
  longer shows either unassociated station card.

2026-08-01 — Railways Tunneling Frontier Inclusion

- Scope: include lines on the Railways list when they have a tunnelable
  frontier, even if they do not yet have a connected frontier.
- Initial branch/status: `main`; `git status --short` already showed
  uncommitted desktop/inspector/pathing/map-framing changes and modified
  runtime/export files (`docs/metro_network.json`, `priority_list.csv`), which
  were preserved.
- Versions: Python 3.13.7; Node v24.11.1; npm 11.6.2.
- Baseline checks before editing: Python compile passed; full
  `python3 -m unittest discover -s tests` passed; `npm run test:paths` passed;
  `npm test` passed.
- Implementation: added a tunneling-frontier segment helper based on
  line-specific tunneled stop membership. The Railways sidebar line inclusion
  now considers both connected frontiers and tunneling frontiers.
- Regression coverage: `tests/test_railway_timing.py` covers a line with one
  tunneled stop and no connected frontier still appearing in the Railways list.
- Checks after editing: `python3 -m unittest tests.test_railway_timing` passed;
  targeted Python compile passed; full `python3 -m unittest discover -s tests`
  passed; `npm run test:paths` passed; `npm test` passed; `git diff --check`
  passed.
- Live-Tk limitation: `PLUMVILLE_RUN_TK_SMOKE=1 npm run smoke:tk` still aborts
  with exit code 134 before app-level output in this execution environment.
- User verification still required: confirm a line appears in Railways when it
  has an adjacent tunnelable next segment but no connected/open segment yet.

2026-08-01 — Desktop Tunneling Follow-Up And Railway Summary Wrap

- Scope: apply the reset/R zoom after the desktop layout settles, rename the
  construction checkbox to Tunneled, persist line-specific tunneling for shared
  stations/segments, refresh Railways percentages after checkpoint edits, and
  shorten the Railways summary text to prevent horizontal clipping.
- Initial branch/status: `main`; `git status --short` already showed
  uncommitted desktop/inspector/pathing/map-framing changes and modified
  runtime/export files (`docs/metro_network.json`, `priority_list.csv`), which
  were preserved.
- Versions: Python 3.13.7; Node v24.11.1; npm 11.6.2.
- Baseline checks before editing: Python compile passed; full
  `python3 -m unittest discover -s tests` passed; `npm run test:paths` passed;
  `npm test` passed.
- Implementation: startup now waits briefly for Tk layout work, runs idle
  tasks, then calls the same `reset_view()` path used by the R hotkey. The
  station construction checklist now says Tunneled. Tunnel saves can target a
  subset of line letters on multi-line stations, persisted in
  `line_tunneled_stop_vars`, so shared geometry can render dashed for one line
  and dotted for another. Railways summaries now mark themselves dirty after
  checkpoint changes, use compact `T:` and `C:` rows, hide the tunneling row at
  100%, and collapse completed lines to `A  C: 100% (7.6 km)` style text.
- Regression coverage: `tests/test_map_framing.py` covers delayed startup
  reset. `tests/test_desktop_inspector.py` covers Tunneled wording.
  `tests/test_path_rendering.py` covers line-specific shared geometry styling.
  `tests/test_railway_timing.py` covers line-specific payload persistence,
  compact summary formatting, completed-line formatting, and Railways dirty
  refresh after checkpoint edits.
- Checks after editing: targeted `python3 -m unittest tests.test_railway_timing
  tests.test_path_rendering tests.test_map_framing tests.test_desktop_inspector`
  passed; targeted Python compile passed; full `python3 -m unittest discover -s
  tests` passed; `npm run test:paths` passed; `npm test` passed; `git diff
  --check` passed.
- Live-Tk limitation: `PLUMVILLE_RUN_TK_SMOKE=1 npm run smoke:tk` still aborts
  in this execution environment, now reporting
  `sandbox_extension_issue_file_to_process failed ... Python.app: 3`.
- User verification still required: confirm startup opens at the same framing
  as pressing R after load, Tunneled wording appears, multi-line stations ask
  which line letters were tunneled, Railways percentages update immediately,
  and the Railways summary no longer bleeds off the sidebar.

2026-08-01 — Desktop Tunneling And Line Hotkeys

- Scope: add Option+letter line-fit hotkeys, station tunneling before
  connection, solid/dashed/dotted metro segment rendering, Railways tunneling
  and completion summaries, and an off-by-default non-orthogonal segment
  highlight.
- Initial branch/status: `main`; `git status --short` already showed the
  uncommitted desktop/inspector/pathing/map-framing changes and modified
  runtime/export files (`docs/metro_network.json`, `priority_list.csv`), which
  were preserved.
- Versions: Python 3.13.7; Node v24.11.1; npm 11.6.2.
- Baseline checks before editing: Python compile passed; full
  `python3 -m unittest discover -s tests` passed; `npm run test:paths` passed;
  `npm test` passed; `PLUMVILLE_RUN_TK_SMOKE=1 npm run smoke:tk` aborted with
  exit code 134 and no app-level diagnostic output.
- Implementation: Option/Alt/Mod1 plus a letter now fits the complete planned
  geometry for that line. Station construction now stores `is_tunneled`;
  connected stations imply tunneled for backward compatibility. The inspector
  shows Tunneling before Connected, and hides Connected until tunneling is set.
  Metro segments render solid when connected, dashed when tunneled, and dotted
  when planned. The Railways sidebar lists started/frontier lines, includes
  tunneling and connection percentages, hides the tunneling percentage when it
  reaches 100%, and sorts completed lines to the bottom. Show/Hide now includes
  a non-orthogonal segment highlight.
- Regression coverage: `tests/test_map_framing.py` covers Option-letter line
  fitting. `tests/test_path_rendering.py` covers solid/dashed/dotted segment
  rendering and non-orthogonal highlighting. `tests/test_railway_timing.py`
  covers tunneling/connection summaries and completed-line sorting.
  `tests/test_desktop_inspector.py` covers Tunneling hiding Connected until
  needed.
- Checks after editing: targeted `python3 -m unittest tests.test_map_framing
  tests.test_path_rendering tests.test_railway_timing
  tests.test_desktop_inspector tests.test_core_network
  tests.test_legacy_path_metadata` passed; targeted Python compile passed; full
  `python3 -m unittest discover -s tests` passed; `npm run test:paths` passed;
  `npm test` passed; `git diff --check` passed.
- Live-Tk limitation: `PLUMVILLE_RUN_TK_SMOKE=1 npm run smoke:tk` still aborts
  with exit code 134 before app-level output in this execution environment.
- User verification still required: confirm Option+A/other letters fit the
  full line, station Tunneling/Connected progression saves properly, planned
  segments look dotted while tunneled segments look dashed, the Railways list
  sorting feels useful, and the non-orthogonal highlight catches diagonal
  planned geometry without clutter when off.
- Follow-up note: line-specific tunneling for geometrically shared segments is
  tracked separately because the current implementation records tunneling at
  station endpoints rather than as independent per-line segment state.

2026-07-30 — Desktop Pathing And City-Limit Inspector

- Scope: move selected path-node and city-limit editing into the docked
  inspector, make path nodes visible only while map pathing is active, infer
  the active town from selection or zoomed-in map center, and add suggested
  city-limit polygons.
- Initial branch/status: `main`; `git status --short` already included
  uncommitted desktop/priority/map-framing changes plus modified runtime/export
  files (`docs/metro_network.json`, `priority_list.csv`), which were preserved.
- Versions: Python 3.13.7; Node v24.11.1; npm 11.6.2.
- Baseline checks before editing: `python3 -m py_compile ui_extensions.py
  desktop_improvements.py path_detection.py metro_station_extensions.py
  legacy_core.py plumville/desktop/inspector.py plumville/desktop/workspace.py`
  passed; full `python3 -m unittest discover -s tests` passed; `npm run
  test:paths` passed; `npm test` passed.
- Implementation: map pathing mode now drives path-node visibility. The docked
  inspector renders selected path-node details/actions, active pathing town
  context, town correction, and city-limit controls. Entering city-limit edit
  mode starts a pending boundary from saved limits or suggested town-owned
  nodes. Node clicks toggle unordered membership, the polygon order is derived
  automatically, and Confirm saves through the existing station city-limit
  record path.
- Regression coverage: `tests/test_city_limit_pathing.py` covers town
  inference, zoom notice conditions, suggested town nodes, pending edit state,
  and confirm saves. `tests/test_desktop_inspector.py` covers selected-node
  inspector controls and zoom/town notice. `tests/test_desktop_improvements.py`
  covers suppressing the old path-node popup. `tests/test_path_rendering.py`
  covers automatic node visibility from map pathing mode.
- Checks after editing: targeted `python3 -m unittest
  tests.test_city_limit_pathing tests.test_path_rendering
  tests.test_desktop_inspector tests.test_desktop_improvements` passed;
  targeted Python compile passed; full `python3 -m unittest discover -s tests`
  passed; `npm run test:paths` passed; `npm test` passed.
- Live-Tk limitation: `PLUMVILLE_RUN_TK_SMOKE=1 npm run smoke:tk` and
  `python3 plumville_app.py` still abort with exit code 134 before app-level
  output in this execution environment.
- User verification still required: confirm map pathing automatically shows
  nodes, selected-node actions appear in the inspector, inferred town can be
  corrected, suggested city limits preview and can be edited by toggling nodes,
  and Confirm persists the polygon.

2026-07-30 — Inspector Map Framing

- Scope: keep the map visually anchored when the docked inspector opens or
  closes, and frame selected stations to a 150-block radius.
- Initial branch/status: `main`; `git status --short` already showed
  uncommitted priority-list changes from the prior run, and those were
  preserved.
- Versions: Python 3.13.7; Node v24.11.1; npm 11.6.2.
- Baseline checks before editing: `python3 -m py_compile ui_extensions.py
  desktop_improvements.py path_detection.py metro_station_extensions.py
  legacy_core.py` passed; full `python3 -m unittest discover -s tests` passed;
  `npm run test:paths` passed; `npm test` passed.
- Reproduction/root cause from code audit: inspector show/hide only
  `grid()`/`grid_remove()`d the right panel. The map canvas then received a
  new size, while `_on_configure()` kept the previous pan/zoom values and only
  redrew, so the visible world area could shift.
- Implementation: inspector visibility changes now capture the current visible
  plot bounds before the layout change and refit those bounds after the Tk idle
  resize. If a station is selected, the deferred refit frames that station
  instead. Station selection through `_focus_stop()` and direct map clicks now
  uses a 150-block plot-radius frame.
- Regression coverage: `tests/test_desktop_workspace.py` covers deferred
  inspector-toggle refitting and selected-station priority. `tests/test_map_framing.py`
  covers the 150-block selected-station radius and `_focus_stop()` behavior.
- Checks after editing: targeted `python3 -m unittest
  tests.test_map_framing tests.test_desktop_workspace` passed; targeted Python
  compile passed; full `python3 -m unittest discover -s tests` passed; `npm run
  test:paths` passed; `npm test` passed.
- Live-Tk limitation: `PLUMVILLE_RUN_TK_SMOKE=1 npm run smoke:tk` and
  `python3 plumville_app.py` still abort with exit code 134 before app-level
  output in this execution environment.
- User verification still required: confirm toggling the inspector preserves
  the same general map area, and selecting a station zooms to a useful but not
  excessive neighborhood around it.

2026-07-30 — Priority List Line Filter

- Scope: add a narrow Priority List filter for metro lines while preserving the
  existing Need filter and desktop wrapper behavior.
- Initial branch/status: `main`; `git status --short` showed an existing
  partial `legacy_core.py` modification from this run after context compaction.
- Versions: Python 3.13.7; Node v24.11.1; npm 11.6.2.
- Baseline checks before editing: `python3 -m py_compile ui_extensions.py
  desktop_improvements.py path_detection.py metro_station_extensions.py
  legacy_core.py` passed; full `python3 -m unittest discover -s tests` passed;
  `npm run test:paths` passed; `npm test` passed.
- Implementation: the Priority List now has a Line dropdown. Selecting a line
  ignores the Need dropdown and route-priority narrowing, then lists every
  station on that line that still has station-completion tasks. All lines keeps
  the previous Need-filter behavior.
- Regression coverage: `tests/test_priority_filter.py` covers line-filter
  override behavior, map highlighting, and line-filter menu population.
- Checks after editing: targeted `python3 -m unittest tests.test_priority_filter`
  passed; targeted Python compile passed; full `python3 -m unittest discover -s
  tests` passed; `npm run test:paths` passed; `npm test` passed.
- Live-Tk limitation: `PLUMVILLE_RUN_TK_SMOKE=1 npm run smoke:tk` and
  `python3 plumville_app.py` still abort with exit code 134 before app-level
  output in this execution environment.
- User verification still required: confirm the Priority List Line dropdown is
  visible, choosing a line shows its unfinished stations, and All lines restores
  the existing Need filter behavior.

2026-07-29 — V02 Pass 2: Segment Editors In Docked Inspector

- Scope: move the selected metro-segment summary and the turn/endpoint preview
  editors out of map-covering popups when the docked inspector shell exists.
- Initial branch/status: `main`; the worktree already contained modified
  desktop visual files, public metro data, and untracked visual-phase docs/tests
  from prior runs.
- Versions: Python 3.13.7; Node v24.11.1; npm 11.6.2.
- Baseline checks before editing: desktop-related Python compile passed; full
  `python3 -m unittest discover -s tests` passed; `npm run test:paths` passed;
  `npm test` passed.
- Reproduction/root cause: the active turn/endpoint flows were still fixed-size
  `tk.Toplevel` dialogs. The turn editor's option text, custom field, preview,
  and action row could exceed the visible popup area, leaving Save unreachable
  on smaller desktop windows.
- Implementation: `plumville/desktop/inspector.py` now renders selected metro
  segment details in the right inspector, opens temporary inspector task views
  for turn and endpoint editing, keeps Save/Cancel at the top of those views,
  preserves live preview redraws without wiping the task view, and restores the
  normal inspector after save/cancel. `legacy_core.py` uses the inspector views
  when available and retains the old dialogs as fallback. `desktop_improvements.py`
  suppresses the old selected-segment floating popup when the docked inspector
  is active.
- Regression coverage: `tests/test_desktop_inspector.py` now covers selected
  segment rendering, task-view persistence during redraw sync, and both Save
  buttons calling the existing write-through helpers. `tests/test_desktop_improvements.py`
  covers selected-segment popup suppression.
- Checks after editing: targeted Python compile passed; targeted
  `python3 -m unittest tests.test_desktop_inspector tests.test_desktop_improvements`
  passed; full `python3 -m unittest discover -s tests` passed; `npm run
  test:paths` passed; `npm test` passed; `git diff --check` passed.
- Live-Tk limitation: `PLUMVILLE_RUN_TK_SMOKE=1 npm run smoke:tk` and
  `python3 plumville_app.py` still abort with exit code 134 before app-level
  output in this execution environment.
- User verification still required: confirm selecting a metro segment fills the
  inspector, Add/Edit Turn and Edit Endpoints replace the inspector content
  until Save/Cancel, Save remains visible, and real data updates persist.

2026-07-29 — Desktop Visual Run V01: Workspace Shell

- Scope: replace the old single-sidebar root layout with a real desktop shell:
  top app bar, compact mode rail, secondary panel host, flexible map host,
  empty docked inspector shell, and optional status strip.
- Required visual audit before editing: read all three mockup PNGs in
  `data/mockups/` and audit the current Tk layout path in code. The current
  structure was confirmed to still be `sidebar_container + sidebar_canvas +
  sidebar` on the left with the map canvas packed directly on the root.
- Baseline checks before editing: `python3 -m py_compile ui_extensions.py
  desktop_improvements.py path_detection.py metro_station_extensions.py`
  passed; full `python3 -m unittest discover -s tests` passed; `npm run
  test:paths` passed; `npm test` passed.
- Live-Tk limitation in this execution environment: `PLUMVILLE_RUN_TK_SMOKE=1
  npm run smoke:tk` exited 134 and `python3 plumville_app.py` exited 134, so a
  current desktop screenshot could not be captured here. That item remains
  `[blocked]` for this run.
- Implementation: `plumville/desktop/workspace.py` now owns the workspace
  shell frames and mode rail. `legacy_core.py` now exposes a minimal
  host-creation hook so the existing sidebar container mounts into a dedicated
  secondary panel host and the existing map canvas mounts into a dedicated map
  host. `desktop_improvements.py` now uses the new shell as a bridge for the
  existing mode model, top-bar status, mode rail, status strip, and inspector
  visibility without duplicating the old sidebar mode controls.
- Regression coverage: `tests/test_desktop_workspace.py` now covers one-time
  region creation, mode rail installation, mode-sync status updates, and
  inspector hide/show behavior. Existing desktop and entrypoint tests remained
  green.
- Checks after editing: targeted `python3 -m unittest
  tests.test_desktop_workspace tests.test_desktop_improvements
  tests.test_entrypoints_and_public_output` passed; full `python3 -m unittest
  discover -s tests` passed; `npm run test:paths` passed; `npm test` passed;
  `git diff --check` passed. Tk smoke and direct app launch still exit 134 in
  this environment.
- User verification still required: confirm the shell visually matches the
  mockup hierarchy in the real desktop app, confirm the inspector can collapse,
  and confirm resizing remains practical on an actual desktop display.

2026-07-29 — V02 Pass 1: Docked Station Inspector Handoff

- Scope: begin Visual Run V02 by moving persistent selected-station details
  out of the floating map popup and into the docked right inspector host.
- Implementation: added `plumville/desktop/inspector.py` to render empty and
  selected station states, including station name/abbr/coordinates, a large
  station diamond, circular line badges, construction checkboxes, signage,
  reminders, and grouped action rows inside the docked inspector.
- Compatibility: `desktop_improvements.py` now suppresses the persistent map
  popup when the docked shell is active and refreshes the inspector after each
  redraw, while the old popup implementation remains available as fallback when
  the workspace shell is absent.
- Regression coverage: added `tests/test_desktop_inspector.py` and extended
  `tests/test_desktop_improvements.py` for the popup-suppression path.
- Checks after editing: desktop-related Python compile passed; targeted
  `python3 -m unittest tests.test_desktop_inspector tests.test_desktop_workspace
  tests.test_desktop_improvements` passed; full `python3 -m unittest discover
  -s tests` passed; `npm run test:paths` passed; `npm test` passed;
  `git diff --check` passed.
- User verification still required: confirm the old popup no longer blocks the
  map in the live desktop app, confirm the inspector hierarchy feels close to
  the mockup, and confirm the docked content is not too tall or cramped on a
  real desktop window.

  vertical space and still reads clearly in the live desktop app.

======================================================================
AUDIT OF WHAT CODEX HAS ALREADY COMPLETED
======================================================================

AUTOMATED / CODE-PRESENT WORK

[x] Extension application has idempotence safeguards and regression tests.
[x] Desktop route auto-fit logic exists.
[x] A persistent Fit Route action exists.
[x] Short-route fit protection exists.
[x] Path detection is hidden from the normal station popup in code.
[x] Advanced / Experimental path-detection access exists.
[x] Completed-worldgen hide/restore logic exists.
[x] Top-level desktop modes exist:
    - All
    - Explore
    - Directions
    - Construction
    - Edit
    - World
    - Advanced
[x] Mode-specific section filtering and default expansion logic exists.
[x] Desktop abbreviation load/search/display support exists.
[x] Desktop workspace shell code now exists:
    - top application bar
    - compact mode rail
    - secondary panel host
    - map host
    - empty docked inspector shell
    - optional compact status strip
[x] MMCP color constants and first-pass control styling exist.
[x] Circular line-badge helpers exist.
[x] Desktop diamond marker helpers exist.
[x] Selected-station popup styling has received several passes.
[x] Shared dialog auto-styling hooks exist.
[x] Desktop alignment-ellipse management exists.
[x] Desktop metro-turn editing exists.
[x] Desktop metro-segment endpoint editing exists.
[x] Public read-only checklist and priority panels exist.
[x] Public optional city-limit, alignment, and frontier overlays exist.
[x] Extensive Python and public smoke-test coverage was added.

WORK THAT EXISTS BUT IS NOT VISUALLY COMPLETE

[verify] Desktop MMCP styling needs live review.
[verify] Dialog styling needs live review.
[verify] Desktop station diamonds need live review.
[verify] Desktop line badges need live review.
[verify] Mode switching needs live review.
[verify] Selected-station popup layout needs live review.
[verify] Route auto-fit and Fit Route need continued regression checking.
[verify] Completed-worldgen control hiding needs continued regression checking.
[verify] Alignment, turn, and endpoint editors need live workflow review.
[verify] Public desktop live-resize behavior still needs a real browser check.

NOT COMPLETED

[verify] Real multi-region desktop workspace shell
[verify] Dedicated left mode rail
[verify] Dedicated secondary mode panel
[ ] Right docked inspector
[ ] Inspector tabs
[ ] Explicit reusable Tk component library
[ ] Explicit dialog templates
[ ] Abbreviation validation and duplicate warnings
[ ] Construction administration workspace
[ ] Canonical/public data separation
[ ] Performance measurement for the new desktop UI

======================================================================
WHY THE CURRENT RESULT STILL DOES NOT MATCH THE MOCKUPS
======================================================================

The current work mostly restyles and filters the existing application.

Current structural approach:

- one existing sidebar;
- a mode selector that hides or shows old sidebar sections;
- floating selected-station popup;
- recursive widget-tree dialog styling;
- decorative station-diamond overlays after redraw.

Mockup structural approach:

- clear top application bar;
- dedicated left mode rail;
- mode-specific secondary panel;
- map as the center workspace;
- persistent right docked inspector;
- explicit component hierarchy;
- explicit dialog layouts;
- selection details outside the map.

The largest remaining gap is structural, not color selection.

Do not spend another run adjusting popup padding, title width, or badge size
before implementing the workspace shell and docked inspector.

======================================================================
CURRENT FUNCTIONAL STATUS
======================================================================

REPORTED OR WELL-COVERED

[x] Private desktop app startup has been repaired for the reported Tk issue.
[x] Route planning remains implemented.
[x] Route fit behavior has automated coverage.
[x] Path-detection relocation has automated coverage.
[x] Worldgen completion decisions have automated coverage.
[x] Public viewer validation and smoke tests remain present.
[x] Desktop abbreviations load, search, and display in code.
[verify] Station edit dialogs can save a station name and optional abbreviation,
    with blank abbreviation removing the saved abbreviation.
[x] Metro line path reset and turn helpers have tests.
[x] Alignment and endpoint helper logic has tests.
[x] Priority list can filter to one metro line and show all unfinished stations
    on that line.
[x] Inspector open/close preserves the current map center, and selected
    stations frame to a 150-block radius when selected.
[x] Desktop map pathing shows nodes automatically, moves selected-node and
    city-limit editing into the inspector, and supports suggested city-limit
    polygons.
[verify] Desktop inspector starts hidden, reopens for new tasks, supports scrolling
    and revised station/sign/status layout, default reset-view startup, convex city
    limits, station-entry map markers, and simplified route modes.
[verify] Desktop inspector and sidebar refinement: use the default reset view on
    startup, preserve map center when the inspector changes width, show
    unfinished station needs in Status, tighten sign layout, move pathing entry
    points, and exclude unnamed stations from the priority list.
[verify] Add Option+letter line zoom, station tunneling state with tunneled/dotted
    line rendering, railway tunneling/completion summaries, and a show/hide
    highlight for non-orthogonal planned geometry.
[verify] Add line-specific tunneling choices for geometrically shared segments.
[verify] Fix startup reset-view timing, Tunneled wording, shared-line tunneling,
    Railways summary wrapping/live refresh, and compact Railways summary text.
[verify] Railways list includes tunneling frontier lines, not only connected
    frontier lines.

NEEDS ANDREW'S LIVE VERIFICATION

[ ] Mode selector appears in the intended location.
[ ] All mode starts with sections collapsed.
[ ] Every focused mode opens the intended section.
[ ] Station popup looks correct.
[ ] Station diamonds look correct.
[ ] Station inspector line diamonds are right-justified and ordered correctly.
[ ] Dialogs remain usable.
[ ] Abbreviation search works in the live desktop app.
[ ] Alignment editor works with real data.
[ ] Turn editor works with real data.
[ ] Endpoint editor works with real data.
[ ] Priority List Line filter shows selected-line unfinished stations and All
    lines restores the Need filter.
[ ] Priority List Line filter does not show unnamed placeholder stations.
[ ] Inspector open/close keeps the same map center visible.
[ ] Selecting a station frames it at roughly a 150-block radius.
[ ] Map add/link mode automatically shows path nodes, and turning it off hides
    them.
[ ] Selecting a path node shows node details and path actions in the inspector.
[ ] Pathing town detection chooses the correct nearest station, and Change Town
    can correct it when needed.
[ ] City-limit mode suggests a boundary from town nodes when none is saved.
[ ] City-limit suggestions form one convex outline around town nodes.
[ ] The inspector starts hidden, can be reopened with the top Show Inspector
    control, and pops open again for a new selected task after being hidden.
[ ] Tall inspector content scrolls vertically.
[ ] Station signs show equal-width non-directional sign cells without clipped
    text.
[ ] Station entry dots and dotted station-entry connector lines appear only
    when zoomed in enough.
[ ] Directions use the Metro/Walking travel dropdown, with flying only as a
    fallback for disconnected route portions.
[ ] Station inspector Status lists unfinished needs instead of a progress
    fraction.
[ ] Checked construction checkboxes have muted text, while unchecked items stay
    bright and readable.
[ ] The station inspector Paths button enables map add/link mode for that town.
[ ] The Pathing sidebar Add Path button enables intercity path add/link mode.
[ ] Railways sidebar no longer shows finishing mode, finish coordinates, or a
    Save Point action.
[ ] Railways progress reads connected track length against planned track length.
[ ] Line order reversal remains a separate follow-up before changing generated
    unnamed station numbering.
[ ] City-limit node toggles can be selected in any order, and Confirm saves a
    sensible polygon.
[ ] Completed worldgen UI hides only intended controls.
[ ] Desktop public viewer survives live browser resizing.

======================================================================
LOWER-PRIORITY BACKLOG
======================================================================

DESKTOP ABBREVIATIONS

[verify] Edit abbreviations in station dialogs.
[ ] Validate 2–5 alphanumeric characters.
[ ] Preserve explicit user capitalization.
[ ] Warn on duplicates.
[ ] Never silently overwrite.
[ ] Preserve abbreviations through rename, history, undo, and export.

CONSTRUCTION

[ ] Shared construction calculations.
[ ] Private construction administration view.
[ ] Sanitized public construction contract.
[ ] Public desktop-browser construction dashboard.

DATA ARCHITECTURE

[ ] Make data/ the canonical editable data folder.
[ ] Add a safe canonical/public export workflow.
[ ] Add parity validation.
[ ] Preserve backward compatibility during migration.

STRUCTURAL CLEANUP

[x] Remove obsolete visual-refresh/audit prompt docs from the repository root
    while preserving active task docs and the mockup images.
[ ] Update README architecture.
[ ] Update file classification.
[ ] Reduce monkey-patching through stable hooks.
[ ] Continue tested legacy_core.py extraction.
[ ] Move desktop UI into plumville/desktop/ in staged slices.
[ ] Measure before optimizing.

======================================================================
HOLD AND DEFERRED
======================================================================

[hold] Phone portrait-to-landscape Fit centering
[hold] New phone/mobile navigation
[hold] Phone/mobile bottom sheet
[hold] Phone safe-area refinements
[hold] Additional phone touch-layout work
[deferred] Stable station IDs separate from line-membership vars
[deferred] Path-detection repair or retirement decision
[deferred] Water, river, and coastline work

======================================================================
NONNEGOTIABLE SAFETY
======================================================================

- Preserve all station names.
- Preserve all line names and the A–Z system.
- Preserve dotted/planned and solid/open meaning.
- Preserve ignored/private worldgen and path-detection data.
- Preserve Docker and LAN workflows.
- Preserve current entry points.
- Never expose private desktop data in docs/.
- Never modify real runtime data merely to satisfy a test.
- Never commit or push without Andrew's explicit request.
