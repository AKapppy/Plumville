Minecraft Metro Map Notes
=========================

#App (Python Version)
   [] it opens really slowly. i know this is because of the immense image size, but is there a way to speed this up?
   [] each step takes so long to finish. i waited over 10 minutes before force canceling the 1st step
   [] can the displayed yellow box (we can change the color it doesn't matter) be the borders of the rendered map still? the percentage should be based on that box not the full full box
   [] if it's possible and won't add too much time to the process, i would like it to show pixels as they are processed, so i can see more continuous progress. honestly, we can do the whole thing on a pixel-by-pixel basis. just scan the box for blank pixels based radially around blackport, following the path of a spiral. it should scan to see if the pixel is accounted for. if it is, no need to do anything. just a super quick check-and-skip. if it's empty, then it uses the renderer to find what that pixel is, display it, then move onto the next pixel. i hope this might make the process smoother and make it go quicker. this would eliminate the entire concept of the blue square

#App (Web Version)

#World Map
   [] this should be a completely virtual experience. no minecraft world will be opened at anytime to generate the terrain. it should just be based off the seed and the seed alone. if docker desktop is looking for an open or saved minecraft file, it will never find one. we should reroute it if this is the case
   [] keep filling the rectangle with repeated Auto Fill Step clicks.
      - reset backup: `worldgen_data/reset_backups/20260416_094729`
      - active generated world folder: `metro_map_world_boxfill`
      - render area is centered on Blackport at x=294, z=390
      - Auto Fill now starts at Blackport/world spawn only; no teleport targets are sent
      - latest fresh Blackport-only load cached 545 chunk columns
      - latest fresh render reads 545 chunk columns and draws 139520 known-color pixels
      - latest fresh report found 1116 sampled uncolored occurrences
      - Auto Fill now advances through least-covered rectangular targets with zero configured overlap
      - next target rectangle is drawn in blue around the next rectangular load target
      - latest expansion pass targeted (294, 246), cached 760 chunk columns, and rendered 194560 known-color pixels
      - next expansion target preview draws from a 64-target rectangular pool
      - coverage now includes cached headless packet columns as well as LevelDB columns
      - Auto Fill is the only world-map button; it runs one batched Bedrock load session, then renders
      - current load session settings: wait 75 seconds max, 4 teleport targets, chunk radius 16, zero-block target overlap
      - default gray / unknown blocks are now transparent instead of rendered, so the map is intentionally sparse until more network palette IDs are mapped back to block names
      - unknown/uncolored block details are written to `worldgen_output/uncolored_blocks_report.txt` after each render
      - headless loader now saves `headless_start_game.json` so the next renderer pass can inspect whether Bedrock is using hashed block network IDs and which block names were advertised
      - do not repair or edit the live Bedrock DB before Auto Fill; the old recovered world crashed after repeated restarts plus live DB mutation


Future Items
------------
[x] World map: expand chunk loading across the full requested render area, not just the first patch around Blackport.
[] World map: if server-side teleports do not persist new chunk areas, make the headless client move itself or reconnect at the next target.
[] World map: add a focused preview or "zoom to rendered area" helper if the full-radius render stays hard to inspect.
[] World map: improve the terrain palette after coverage is reliable.
[] World map: consider moving generated world data out of iCloud Drive if performance or sync weirdness becomes a problem.
[] World map: remember that changing the seed later will not magically regenerate an existing saved world.
[] World map: remember that Bedrock only saves chunks that have actually been generated/visited.
