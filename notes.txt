Minecraft Metro Map Notes
=========================

Desktop App
-----------
|- we need an "edit note" button in the popup UI. this would just move the current node to a different coordinate input by the user. it would keep all connections to/from that node in tact, just move them
|- when paths are drawn using click-and-drag, it shouldn't leave the path highlighted. it should automatically go to the default dotted lines once the hold is released
|- node UI behavior should match station UI behavior - where it kinda sticks to the bottom left of the screen. can we put a little x on those popups to close it
|- the UI seems to be a little laggy. can we try to smooth it out and speed it up?
|- hide nodes doesnt actually hide the nodes from view
|- "paths detected" in the checklist should be changed to the village's walking paths, the checkbox in the villages UI
|- we also need a checklist for the village boundaries

Web Viewer
----------

World Map / Generation
----------------------
|- Improve the terrain palette after coverage is reliable. Coverage is now 100%
|- If server-side teleports stop persisting chunk areas, make the headless client move itself or reconnect at the next target.
|- Remember that changing the seed later will not regenerate an existing saved world.
|- Remember that Bedrock only saves chunks that have actually been generated or visited.
|- figure out how to connect to live playstation server that i would be playing in to get a map of the actual world and not just the generic seed im in

Cleanup Candidates
------------------
|- Keep an eye on patch modules such as worldgen_speedups.py and world_map_overrides.py; consolidate only when their behavior is stable.
|- Leave small public launch wrappers in place unless there is a clear replacement entrypoint.
