M02 RESUME NOTE — 2026-09-15

Automated checks remain passing apart from four baseline signage cases. Final
runtime verification is qualified: private network-backup mtime differs by +74 ns
since initial scan, size unchanged; no other ignored-file or tracked-data change.
No real-data executable probe was run on resume. Cause unestablished; see the M02
report. No private data repair/rewrite is authorized by this observation.

M02 LOCAL UPDATE — 2026-09-14

1. M03 — worldgen read-only/status safety. Live/CLI status still calls
   ensure_layout and writes environment state. Do not probe real runtime yet.
2. M02 automated acceptance passes: read-only network import/load, deterministic
   memberships, validated atomic canonical saves, preserved explicit history,
   explicit CSV export and in-memory terrain previews. GUI checks stay pending.
3. Run checks via scripts/run_isolated_tests.py from the repository root. It
   creates disposable source/public fixtures and synthetic config; private data
   is excluded. Do not use M01's old /tmp copy as a source of truth.
4. Later: stable Swift interchange contract. No Swift work in this repository.

M02 baseline: 291 tests, 2 signage failures/2 errors. Final: 304 tests, the same
four problems. 54 focused tests, compile, npm path/public checks and diff check
pass. No real/private runtime data changed. See M02_PERSISTENCE_REPORT.md.

Historical M01 priorities and visual notes below are retained for context.

M01 LOCAL AUDIT — 2026-09-14

This section supersedes the historical visual ordering below for migration
readiness follow-up. Source: local main at b6c26cf, not a GitHub fetch.

1. Read-only data loading/test isolation: legacy_core.py:15473,15486 and final
   import-time reload; core/network.py history writer. Baseline probes caused
   order-only saves and private history rotation. No further real-data testing
   until this boundary is safe; consult M01_AUDIT_REPORT.md.
2. Review MIGRATION_INVENTORY.md and M01_AUDIT_REPORT.md. They map current
   ownership, local sizes, extension order and portable versus UI/backend work.
3. Investigate four baseline station-signage problems using synthetic fixtures.
4. Resume existing live desktop verification using disposable data; Priority
   List wrapping, path editing/connect/snap and inspector checks remain open.
5. Later: separate domain mutations from persistence, consolidate proven shared
   helpers, measure redraw/inspector/terrain costs and verify clean dependencies.

M01 cleanup: removed only the path-detection no-op statistics wrapper; 64
focused tests pass. Full suite: 291 tests, unchanged 2 failures/2 errors. Python
compile, path suite, npm checks and diff check pass. Tk launches abort here.
No Swift/Xcode, account, cloud, schema or broad desktop rewrite performed.

CURRENT SLICE UPDATE — 2026-09-13 — D01-P

Priority List wrapping implemented; awaiting live desktop verification.
- legacy_core.py: wrap summary, entries, and empty message to allocated width.
- tests/test_priority_filter.py: resize, padding, empty-state, and click coverage.
- Priority tests pass; full suite retains four baseline signage problems.
- Tk smoke/app launch abort with exit 134 here; verify All needs / Line B and
  resizing on desktop before moving to another major slice.

PLUMVILLE FILE PRIORITY LIST

Last audited against GitHub main: 2026-07-29
Audited commit: 03f67070beb8224e426a575fd905b791d496810a

======================================================================
CURRENT PRIORITY ORDER — VISUAL PHASE
======================================================================

1. New desktop inspector module under plumville/desktop/
2. New desktop shell module under plumville/desktop/
3. Small stable hooks in legacy_core.py
4. desktop_improvements.py
5. Focused desktop UI tests
6. metro_station_extensions.py
7. ui_extensions.py
8. Dedicated desktop theme/components module
9. Dialog-owner files
10. docs/app.js and docs/styles.css — desktop regressions only
11. Data architecture files
12. Documentation

Current documentation cleanup state:

- obsolete root-level visual refresh, audit, and AI prompt files from July 2026
  have been removed after confirming they only referenced each other;
- active repo instructions, run trackers, runtime/private data, exports,
  history, and `data/mockups/` were preserved.

======================================================================
1. NEW DESKTOP INSPECTOR MODULE
======================================================================

Recommended path:

- plumville/desktop/inspector.py

Purpose:

- own the first real docked-station inspector implementation;
- replace persistent selected-station popup content;
- provide empty and selected states inside the right docked region;
- keep station actions out of map-covering popups.

Current V02 state:

- `plumville/desktop/inspector.py` now exists and owns the first docked station
  inspector pass;
- the active desktop shell now sends persistent station detail rendering into
  the docked inspector instead of the floating popup;
- the inspector currently shows an empty state, station header block, line
  badges, construction controls, signage, reminders, and grouped actions;
- selected metro-segment details now render in the docked inspector instead of
  the floating map popup when the shell is active;
- metro turn and endpoint preview editors now use temporary inspector task
  views with reachable Save/Cancel actions, while retaining the old dialogs as
  fallback when the shell is absent;
- inspector tabs and a dedicated danger region are still pending.

Current map-framing state:

- inspector visibility changes preserve the current map center after the right
  panel opens or closes;
- selected stations intentionally frame to a 150-block radius around the
  station point when they are selected.

Current desktop pathing state:

- selected path-node details/actions render in the docked inspector instead of
  the map popup when the desktop shell is active;
- the inspector starts hidden, reopens automatically for new selected tasks
  after task-local hides, and now has a scrollable body for tall task content;
- inspector show/hide now refreshes the post-layout canvas size and recenters
  the same world point that was at the viewport center before the toggle,
  without changing zoom;
- the station header now uses abbreviation-first title styling, right-aligned
  line-colored diamond badges, simplified coordinates, and reduced status
  noise; inspector line diamonds now use white borders and white letters for
  contrast on the dark dock background;
- station Status now lists unfinished priority-style needs instead of a raw
  progress fraction;
- station entry and city-limit completion now appear as automated construction
  checks instead of status text; station entry appears after facade completion,
  and city limits appear after walking paths are complete;
- the docked signage section now keeps controls at the top and lays out
  non-directional signs as raised equal-width cells before directional town
  lists;
- station inspector actions now order Edit as Name, Coords, Station Entry and
  Manage as Lines, Paths, Alignments;
- the Pathing sidebar now exposes Path nodes and Suggested paths overlay
  toggles; pathing mode still turns nodes on by default, but the node overlay
  can be hidden while pathing remains active;
- suggested walking paths now reuse cached terrain metadata between unchanged
  redraws, avoiding repeated terrain-grid rebuilds after the first suggestion
  pass;
- adding a path node on an existing drawn walking path now splits that walk
  edge into two walk edges through the new node, while connector edges are
  preserved;
- selected path-node actions now show Connect, Edit, and Remove without the
  Metro Path action, and coordinate edits merge into an existing node while
  keeping the pre-existing node label;
- saved path-node IDs now normalize to positive numeric `node_<number>` values
  with labels, PoI names, categories, and coordinates kept separate; referenced
  coordinate endpoints are materialized as saved numeric nodes, new nodes reuse
  the lowest open numeric slot, and the selected-node inspector can remove
  individual path connections;
- suggested walking paths now show the immediate frontier around the existing
  walking-path network, search a larger direct-frontier endpoint pool for
  lower-water routes, and draw in yellow for clearer contrast;
- suggested walking paths now keep one best direct route per frontier village,
  split useful alternates into shared trunks, can connect into drawn path
  midpoints, and can route through isolated existing walk segments when those
  reduce the missing pathing needed;
- desktop and public web suggested walking paths now share the same generated
  segment data and yellow line-segment styling; generated sloped suggestions
  keep only integer or reciprocal-integer slopes, splitting rational slopes
  into Minecraft-buildable segments;
- suggested walking paths now grow as a branch tree from the existing walking
  network, using walk-ready intermediate villages as later branch sources,
  keeping non-walk-ready villages as leaves, and consolidating close coordinate
  branch points so nearby suggestions share a clear path source;
- city-limit editing uses inspector controls, inferred/correctable town
  context, pending node selection, and confirm-to-save convex polygons.
- Path Nodes now include an Active Coordinates option for using the visible
  map crosshair as Add Node input; its Align option appears only while active,
  defaults on, and snaps near an existing path-node row or column.
- The map canvas now supports arrow-key nudging for the visible coordinate
  crosshair while the canvas has focus.
- Selected path-node Connect now opens typed endpoint and Click to Select
  choices; Click to Select keeps the original node as the source while clicked
  target nodes are connected back to it, and empty map clicks do not create
  new nodes in that mode.
- Station signage direction headers now act as map navigation controls,
  selecting and framing the nearest next station on the active line in the
  displayed direction.
- The Pathing sidebar now exposes an explicit Editing Mode button for map-click
  node creation and node-to-node path dragging; the old visible path-node
  coordinate/label Add Node form has been removed from the normal sidebar.
- Editing Mode empty-click node creation is suppressed for popup button
  releases, and dragging from non-node map space continues to pan the map.

Do not:

- rebuild the workspace shell here;
- redesign dialogs in the same run;
- move unrelated editing features into the inspector run.

======================================================================
2. NEW DESKTOP SHELL MODULE
======================================================================

Current state:

- `plumville/desktop/workspace.py` now exists and owns the V01 shell frames;
- it mounts the legacy sidebar container into a dedicated secondary panel host;
- it mounts the existing map canvas into a dedicated map host;
- it provides a compact mode rail, top bar, empty inspector shell, and
  optional status strip;
- the right inspector panel starts hidden while its top-bar Show/Hide control
  remains available;
- inspector visibility changes preserve the viewport center across right-panel
  width changes instead of refitting selected stations or prior bounds;
- mode selection now lives in a top-bar mode dropdown, and the old left mode
  rail is hidden while the existing `install_mode_rail()` bridge remains
  idempotent;
- it still needs Andrew's live verification before further shell cleanup.

======================================================================
3. SMALL STABLE HOOKS IN legacy_core.py
======================================================================

Risk:

- extremely high

Current V01 state:

- V01 added a narrow workspace-host hook so the new shell can own root layout
  without broad constructor rewrites;
- these hooks should remain small and explicit.

Current priority-list state:

- `legacy_core.py` owns the Priority List Need and Line filters;
- selecting a line shows all unfinished stations on that line and ignores the
  Need filter until All lines is selected again, while still excluding unnamed
  placeholder stations;
- selecting a Need filter now lists only matching station names while retaining
  the same map highlights;
- desktop startup now uses the same reset view as the R hotkey;
- station entries render as small zoom-gated dots with dotted gray station
  links and continue to be used as station walking endpoints;
- route planning now exposes a Metro/Walking travel dropdown and uses flying
  only as a fallback when graph routes are unavailable.
- the visible Railways sidebar no longer exposes finishing mode, finish
  coordinate entry, or Save Point; its summary reports connected track length
  against total planned track length.
- Option+letter hotkeys fit the full selected letter line on the map, using
  the complete planned line geometry regardless of completion.
- station construction now records tunneling before connection; map rendering
  distinguishes connected solid, tunneled dashed, and planned dotted segments.
- line-specific tunneled stop membership is persisted in
  `line_tunneled_stop_vars`, so shared geometry can be tunneled on one line
  without forcing the same state on every line using that segment.
- the Railways sidebar includes started or frontier lines with tunneling and
  completion percentages, with completed lines sorted to the bottom; the
  summary uses compact `T:` and `C:` rows to avoid sidebar text clipping, and
  frontier inclusion now considers tunnelable next segments as well as
  connected next segments.
- the Show/Hide section has an off-by-default non-orthogonal segment
  highlight to help find metro geometry that still needs turns.
- the desktop map export button/dialog now says Export PNG and writes
  `metro-map-*.png` from the visible full-resolution world-map crop, then draws
  selected overlays into the block-native PNG with one PNG pixel per source
  block and no native Cairo/CairoSVG dependency.
- desktop map labels now use a web-inspired priority order and abbreviation
  fallback before full labels away from close zoom; selected stations, route
  endpoints, junctions, and terminals are kept ahead of ordinary crowded labels.
- desktop metro lines now draw circular line-letter markers for terminal line
  identity and zoomed-in junction identity.
- desktop map decoration now draws a separate home landmark at `-2556,1340`,
  independent of the nearby Mt. Phosphagos station marker.
- station name detection now treats uppercase station-letter/position labels
  such as `Z_2` and `UW` as unnamed placeholders.
- station Name editing now saves both `lbl` and optional `abbr`; blank
  abbreviation removes the saved abbreviation field.
- empty search no longer shows the long instruction sentence, and coordinate
  searches with no station match center the coordinate with the cursor
  crosshair/readout visible.

Deferred follow-up:

- reverse-line-order editing needs a separate data-safe run because it changes
  line ordering and generated unnamed station numbers.

Next use:

- selected-station change callback;
- inspector refresh callback;
- small shell/inpector lifecycle hooks only if required.

Do not:

- add hundreds of lines of styling;
- put the inspector implementation directly into legacy_core.py;
- rewrite existing builders wholesale.

======================================================================
4. desktop_improvements.py
======================================================================

Current state:

- still large and mixed;
- now bridges the existing mode model into the new workspace shell;
- should continue shrinking in responsibility over later visual runs.

Immediate goal:

- keep compatibility behavior stable;
- let dedicated desktop modules own visual structure;
- avoid adding new shell or inspector implementation detail here unless it is
  truly a bridge concern.

Do not start this in Visual Run V01.

======================================================================
5. FOCUSED DESKTOP UI TESTS
======================================================================

Recommended additions:

- tests/test_desktop_workspace.py
- tests/test_desktop_inspector.py

Test without depending on real runtime data where possible.

Cover:

- region creation;
- mode-to-panel mapping;
- no duplicate control mounting;
- inspector collapse state;
- selected-station synchronization;
- component configuration;
- repeated extension application.

Do not replace live user verification with fake-Tk assertions.

======================================================================
6. metro_station_extensions.py
======================================================================

Use after the inspector exists.

Potential work:

- move station actions into inspector tabs;
- add abbreviation editing;
- add validation and duplicate warnings;
- keep dialogs focused.

Do not use it to build the workspace shell.

======================================================================
7. ui_extensions.py
======================================================================

Keep as a small explicit coordinator.

Responsibilities:

- apply extensions in a documented order;
- install the workspace shell;
- avoid import-time mutation;
- remain idempotent.

Do not put visual implementation here.

======================================================================
8. DESKTOP THEME / COMPONENT MODULE
======================================================================

Recommended path:

- plumville/desktop/theme.py
- plumville/desktop/components.py

Start after the shell geometry is stable.

Move:

- MMCP colors;
- spacing;
- typography;
- button palettes;
- panel builders;
- line badges;
- station diamonds;
- status chips.

Do not spend a run migrating constants before the shell exists.

======================================================================
9. DIALOG-OWNER FILES
======================================================================

After component foundations:

- metro_station_extensions.py
- legacy_core.py dialog methods
- future plumville/desktop/station_editor.py
- future plumville/desktop/line_editor.py

Replace generic recursive styling with explicit templates one flow at a time.

======================================================================
10. PUBLIC VIEWER FILES
======================================================================

Files:

- docs/app.js
- docs/styles.css
- docs/index.html

Allowed during mobile hold:

- reproduced desktop-browser regressions;
- accessibility issues testable locally;
- public data validation.

Do not:

- add new phone features;
- redesign mobile layout;
- use the public viewer as a substitute for desktop shell work.

======================================================================
RECOMMENDED RUN ORDER
======================================================================

V01
- workspace shell only

V02
- docked inspector shell and Overview tab

V03
- remaining inspector tabs and action migration

V04
- component system and secondary-panel cleanup

V05
- explicit dialog templates

V06
- canonical desktop map symbols and semantic labels

Each run must stop for Andrew's live check.

======================================================================
COMPLETED / LOWER-PRIORITY FILE WORK
======================================================================

desktop_improvements.py
- route fit
- Fit Route
- path-detection relocation
- completed-worldgen UI logic
- mode filtering
- first-pass MMCP styling
- popup and marker decoration
- separate desktop home landmark

legacy_core.py
- abbreviation load/search/display
- alignment manager
- metro-turn editor
- endpoint editor
- walking path-node mutation and edge splitting
- compatibility fixes

docs/
- public route and search improvements
- public MMCP styling
- public line filters
- read-only checklist and priority views
- overlay toggles
- exported metro data cleanup: unassociated station records `P_0`, `P_6`, and
  `P_2` were removed from `docs/metro_network.json`, along with their
  priority-list rows and orphaned alignment reminders.

tests/
- extensive unit and smoke coverage for current behavior
